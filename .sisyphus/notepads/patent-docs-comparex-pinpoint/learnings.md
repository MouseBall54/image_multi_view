
- Task 1 validator can stay stdlib-only: argparse + pathlib + regex are enough for required-file, heading, and forbidden-phrase checks.
- Task 2 outline should use plain markdown headings matching the validator's expected `1.`-`13.` and `Appendix A/B` markers so schema checks stay mechanical.
- Wave 2 evidence is strongest when each row maps one behavior to one code anchor and explicitly distinguishes `implemented` from `implemented_context`.
- Figure and docx artifacts work best when they are treated as downstream formatting aids fed by the canonical markdown package, not as independent sources of truth.
- Final-wave reviewers cared about process artifacts too, so package completion needs evidence logs and an explicit final gap log in addition to content files.

- Final rerun evidence is strongest when a fresh validator pass is paired with explicit per-section evidence IDs and a claim-support bridge.
